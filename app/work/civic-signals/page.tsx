import type { Metadata } from "next";
import { CreatingUXPortfolio } from "./CreatingUXPortfolio";

export const metadata: Metadata = {
  title: "Creating UX Design Portfolio Web Page Case Study"
};

export default function CivicSignalsPage() {
  return <CreatingUXPortfolio />;
}
